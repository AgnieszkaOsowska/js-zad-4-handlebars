{
  "name": "parcel-simple-example",
  "version": "1.0.0",
  "description": "Simple example based on parcel",
  "scripts": {
    "start": "parcel index.html --open",
    "production": "parcel build index.html --out-dir public"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1",
    "parcel-bundler": "^1.7.0"
  },
  "dependencies": {
    "handlebars": "^4.0.11"
  }
}
